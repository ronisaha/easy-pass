import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.xiidea.easypass',
  appName: 'Easy Password Generator',
  webDir: 'www'
};

export default config;
